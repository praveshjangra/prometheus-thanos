apiVersion: v1
kind: Service
metadata:
  name: status-web
  namespace: default
  labels:
    app.kubernetes.io/name: status-web
    app: status-web
spec:
  selector:
    app: status-web
  ports:
  - name: http-metrics
    port: 9898
    targetPort: http-metrics
