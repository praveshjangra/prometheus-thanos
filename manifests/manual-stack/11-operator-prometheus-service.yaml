apiVersion: v1
kind: Service
metadata:
  name: prometheus-k8s
  namespace: monitoring-manual
  labels:
    app.kubernetes.io/name: prometheus
spec:
  selector:
    operator.prometheus.io/name: k8s
  ports:
  - name: web
    port: 9090
    targetPort: 9090
